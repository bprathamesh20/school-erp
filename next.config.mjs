/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['qtzuwjbthpskhdghhmkw.supabase.co'], // Allow this domain for images
    },
};

export default nextConfig;
